# Public Assets

Place your logo image here as `logo.png` (or update the path in WelcomeScreen.jsx if using a different name/format).

The logo will be displayed:
- On initial app load with a flash animation
- On the login/welcome screen
- After logout (with flash animation before returning to login)

Supported formats: PNG, JPG, SVG, etc.
